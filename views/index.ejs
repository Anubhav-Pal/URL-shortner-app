<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>URL Shortner</title>
</head>

<body>
    <h1>URL Shortner</h1>
    <form action="/shorturl" method="post">
        <label for="formURL">URL</label>
        <input type="url" id="formURL" name="formURL">
        <button type="submit">Shorten</button>
    </form>
    <br>
    <table border="1">
        <thead>
            <tr>
                <th>Original URL</th>
                <th>Shorten URL</th>
                <th>Clicks</th>
            </tr>
        </thead>
        <tbody>
            <% shortUrls.forEach(shortUrl=>{ %>
                <tr>
                    <td><a href="<%=shortUrl.long%>">
                            <%=shortUrl.long%>
                        </a></td>
                    <td><a href="<%=shortUrl.short%>">
                            <%=shortUrl.short%>
                        </a></td>
                    <td>
                        <%=shortUrl.clicks%>
                    </td>
                </tr>
                <% })%>
        </tbody>
    </table>
</body>

</html>